{
  "name": "governance-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ warlord-dev/governance",
    "deploy-dev": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ warlord-dev/governance",
    "create-local": "graph create --node http://localhost:8020/ warlord-dev/governance",
    "remove-local": "graph remove --node http://localhost:8020/ warlord-dev/governance",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 warlord-dev/governance"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.17.1",
    "@graphprotocol/graph-ts": "0.17.0"
  }
}
